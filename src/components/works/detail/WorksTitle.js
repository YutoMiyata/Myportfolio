import React from 'react'
import "./WorksTitle.css"

const WorksTitle = ({titleName}) => {
  return (
    <div className='works-title'>
      <h1>{titleName}</h1>
    </div>
  )
}

export default WorksTitle
